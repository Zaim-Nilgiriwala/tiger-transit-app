---
phase: 08-training-adaptation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build_differentiator_features.py
  - data/processed/train_featured_v2.parquet
  - data/processed/val_featured_v2.parquet
  - data/processed/test_featured_v2.parquet
autonomous: true

must_haves:
  truths:
    - "Featured parquets contain 45 features (43 original - lateness_now + baseline_s2s + baseline_seg_sum + baseline_eta)"
    - "Featured parquets contain residual as target column alongside time_to_arrival_seconds"
    - "Featured parquets contain baseline_eta for downstream reconstruction"
    - "lateness_now is absent from feature list and output parquets"
    - "Row counts match original splits exactly (no data loss during rebuild)"
  artifacts:
    - path: "scripts/build_differentiator_features.py"
      provides: "Updated feature constants, target, and save logic for v1.1"
      contains: "BASELINE_FEATURE_COLS"
    - path: "data/processed/train_featured_v2.parquet"
      provides: "Training data with 45 features + residual target + baseline_eta"
    - path: "data/processed/val_featured_v2.parquet"
      provides: "Validation data with same schema"
    - path: "data/processed/test_featured_v2.parquet"
      provides: "Test data with same schema"
  key_links:
    - from: "scripts/build_differentiator_features.py"
      to: "data/processed/{split}_featured_v2.parquet"
      via: "save_v2_parquet outputs FEATURE_COLS_V2 + TARGET_COL + KEEP_EXTRA columns"
      pattern: "FEATURE_COLS_V2.*BASELINE_FEATURE_COLS"
    - from: "data/processed/{split}.parquet"
      to: "scripts/build_differentiator_features.py"
      via: "baseline_s2s, baseline_seg_sum, baseline_eta, residual columns from Phase 7 augmented parquets"
      pattern: "baseline_eta|baseline_s2s|baseline_seg_sum|residual"
---

<objective>
Modify the feature pipeline to produce v1.1 featured parquets: 45 features (drop lateness_now, add 3 baseline features), residual as target, and baseline_eta + time_to_arrival_seconds preserved for reconstruction.

Purpose: Phase 8 training requires featured parquets with the v1.1 feature set and residual target. This plan updates the constants and save logic in build_differentiator_features.py, then rebuilds all three splits.
Output: Rebuilt train/val/test_featured_v2.parquet files with 45 features, residual target, and reconstruction columns.
</objective>

<execution_context>
@C:\Users\ryanp\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\ryanp\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-training-adaptation/08-CONTEXT.md
@.planning/phases/08-training-adaptation/08-RESEARCH.md
@.planning/phases/07-baseline-infrastructure/07-01-SUMMARY.md

@scripts/build_differentiator_features.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update feature constants, target, and save logic in build_differentiator_features.py</name>
  <files>scripts/build_differentiator_features.py</files>
  <action>
Modify `scripts/build_differentiator_features.py` in place to update for v1.1. All changes are in the constants and save sections -- no changes to feature computation functions.

**1. Update PHASE3_FEATURE_COLS (remove lateness_now):**
Remove `"lateness_now"` from the list. It should go from 15 to 14 items:
```python
PHASE3_FEATURE_COLS = [
    "distance_to_target", "scheduled_time_to_target", "current_speed",
    "route_progress", "stops_remaining", "stop_index",
    # lateness_now REMOVED (zero variance -- scheduled_eta == eta in EtaSpot data)
    "minutes_since_midnight", "day_of_week", "route_id", "pattern_id",
    "precipitation_mm", "temperature_c", "passenger_load", "is_idle",
]
```

**2. Add BASELINE_FEATURE_COLS constant (after PHASE4_FEATURE_COLS definition):**
```python
# Phase 7 baseline features (from augmented parquets)
BASELINE_FEATURE_COLS = [
    "baseline_s2s",
    "baseline_seg_sum",
    "baseline_eta",
]
```

**3. Update FEATURE_COLS_V2 to include baselines:**
```python
FEATURE_COLS_V2 = PHASE3_FEATURE_COLS + PHASE4_FEATURE_COLS + BASELINE_FEATURE_COLS
```
This should produce exactly 45 features: 14 + 28 + 3 = 45.

**4. Change TARGET_COL from time_to_arrival_seconds to residual:**
```python
TARGET_COL = "residual"
```

**5. Update KEEP_EXTRA to preserve reconstruction columns:**
```python
KEEP_EXTRA = ["stops_away", "route_id", "time_to_arrival_seconds", "baseline_eta"]
```
This ensures `time_to_arrival_seconds` (original target) and `baseline_eta` are kept in the parquets for easy reconstruction during evaluation (final_pred = baseline_eta + predicted_residual). Note: `baseline_eta` appears in both FEATURE_COLS_V2 and KEEP_EXTRA but the save logic deduplicates via the `if c not in out_cols` check.

**6. Remove lateness_now computation from compute_phase3_features (optional but clean):**
The function at line ~822 computes `df["lateness_now"] = df["scheduled_eta_seconds"] - df["eta_seconds"]`. Since lateness_now is no longer in PHASE3_FEATURE_COLS, it won't be saved to the parquet. You can leave the computation in place (harmless) or remove it with a comment. Prefer leaving it in place to minimize diff -- it just won't be included in the output since it's not in FEATURE_COLS_V2.

**7. Update the summary print near the end of main() to reflect the new feature count:**
The print already uses `len(PHASE3_FEATURE_COLS)`, `len(PHASE4_FEATURE_COLS)`, and `len(FEATURE_COLS_V2)` so it will auto-update. No change needed.

**8. Update the verify section at the end of main() that reads back parquets:**
Change `columns=[TARGET_COL]` to read the new target. Since TARGET_COL is now "residual", the verify read will check for the residual column. This is already correct through the constant.

**What NOT to change:**
- Do NOT change any feature computation functions (compute_gps_speed, compute_rolling_speed_features, etc.)
- Do NOT change the process_split function (the baseline columns come from the source parquets via merge, not from computation in this script)
- Do NOT touch train_advanced.py in this task (that's Plan 02)
  </action>
  <verify>
Run: `python -c "import sys; sys.path.insert(0, 'scripts'); from build_differentiator_features import FEATURE_COLS_V2, TARGET_COL, KEEP_EXTRA, PHASE3_FEATURE_COLS, BASELINE_FEATURE_COLS; print(f'Features: {len(FEATURE_COLS_V2)}'); print(f'Target: {TARGET_COL}'); print(f'Phase3: {len(PHASE3_FEATURE_COLS)}'); print(f'Baselines: {BASELINE_FEATURE_COLS}'); print(f'Keep extra: {KEEP_EXTRA}'); assert len(FEATURE_COLS_V2) == 45, f'Expected 45, got {len(FEATURE_COLS_V2)}'; assert TARGET_COL == 'residual'; assert 'lateness_now' not in FEATURE_COLS_V2; assert 'baseline_eta' in FEATURE_COLS_V2; assert 'time_to_arrival_seconds' in KEEP_EXTRA; print('ALL ASSERTIONS PASSED')"`
  </verify>
  <done>
FEATURE_COLS_V2 has exactly 45 features. TARGET_COL is "residual". lateness_now is absent. baseline_s2s, baseline_seg_sum, baseline_eta are present. KEEP_EXTRA includes time_to_arrival_seconds and baseline_eta.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild featured parquets and validate schema</name>
  <files>
data/processed/train_featured_v2.parquet
data/processed/val_featured_v2.parquet
data/processed/test_featured_v2.parquet
  </files>
  <action>
Run the feature pipeline to rebuild all three featured parquets with the v1.1 schema.

**Step 1: Run the pipeline:**
```bash
python scripts/build_differentiator_features.py
```
This will take 5-15 minutes. It recomputes all features from scratch and saves new _featured_v2.parquet files. The pipeline reads from `data/processed/{train,val,test}.parquet` which already contain `baseline_s2s`, `baseline_seg_sum`, `baseline_eta`, and `residual` columns from Phase 7.

**Step 2: Validate the output parquets.**
Write and run a validation script that checks:

1. Each split parquet exists and has the expected row count:
   - train: ~1,206,181 rows
   - val: ~339,561 rows
   - test: ~279,289 rows

2. All 45 features from FEATURE_COLS_V2 are present as columns.

3. `residual` column exists and is the target (non-NaN for all rows, or at least >99%).

4. `time_to_arrival_seconds` column exists (preserved for reconstruction).

5. `baseline_eta` column exists (both as feature and for reconstruction).

6. `lateness_now` column does NOT exist in the output.

7. `baseline_s2s` and `baseline_seg_sum` columns exist.

8. NaN check on baseline features: `baseline_eta` should have 0 NaN (from Phase 7). `baseline_seg_sum` may have a small number of NaN (XGBoost handles NaN features natively). Report NaN counts.

9. Feature count check: parquet should have 45 features + 1 target + extra columns from KEEP_EXTRA (deduped).

10. Print residual statistics (mean, std, min, max) for sanity check against Phase 7 values (train mean should be ~-9.1s, std ~287.4s).

**What to watch for:**
- If the pipeline fails with a KeyError on `residual` or baseline columns, it means the source parquets don't have those columns. This would indicate Phase 7 outputs weren't saved correctly. Check `data/processed/train.parquet` for the expected columns.
- The pipeline takes significant time. If it seems stuck, it's likely on the rolling speed features (the heaviest computation).
  </action>
  <verify>
Run validation: `python -c "
import pandas as pd
import sys
sys.path.insert(0, 'scripts')
from build_differentiator_features import FEATURE_COLS_V2, TARGET_COL, KEEP_EXTRA

splits = {'train': 1206181, 'val': 339561, 'test': 279289}
all_ok = True

for split, expected_rows in splits.items():
    path = f'data/processed/{split}_featured_v2.parquet'
    df = pd.read_parquet(path)
    n = len(df)

    # Row count
    if abs(n - expected_rows) > 100:
        print(f'FAIL: {split} rows={n}, expected~{expected_rows}')
        all_ok = False

    # Features present
    missing = [c for c in FEATURE_COLS_V2 if c not in df.columns]
    if missing:
        print(f'FAIL: {split} missing features: {missing}')
        all_ok = False

    # Target present
    if TARGET_COL not in df.columns:
        print(f'FAIL: {split} missing target {TARGET_COL}')
        all_ok = False

    # Reconstruction columns
    if 'time_to_arrival_seconds' not in df.columns:
        print(f'FAIL: {split} missing time_to_arrival_seconds')
        all_ok = False
    if 'baseline_eta' not in df.columns:
        print(f'FAIL: {split} missing baseline_eta')
        all_ok = False

    # lateness_now absent
    if 'lateness_now' in df.columns:
        print(f'FAIL: {split} still has lateness_now')
        all_ok = False

    # NaN counts
    res_nan = df[TARGET_COL].isna().sum()
    bse_nan = df['baseline_eta'].isna().sum()
    bs2s_nan = df['baseline_s2s'].isna().sum() if 'baseline_s2s' in df.columns else -1
    bseg_nan = df['baseline_seg_sum'].isna().sum() if 'baseline_seg_sum' in df.columns else -1

    print(f'{split}: {n:,} rows, {len([c for c in FEATURE_COLS_V2 if c in df.columns])}/45 features, residual_NaN={res_nan}, baseline_eta_NaN={bse_nan}, s2s_NaN={bs2s_nan}, seg_NaN={bseg_nan}')

    if split == 'train':
        res = df[TARGET_COL]
        print(f'  residual stats: mean={res.mean():.1f}, std={res.std():.1f}, min={res.min():.0f}, max={res.max():.0f}')

if all_ok:
    print('ALL CHECKS PASSED')
else:
    print('SOME CHECKS FAILED')
    sys.exit(1)
"`
  </verify>
  <done>
All three featured parquets rebuilt with 45 features, residual target, time_to_arrival_seconds preserved, baseline_eta preserved, lateness_now absent. Row counts match originals. Train residual mean ~-9s, std ~287s.
  </done>
</task>

</tasks>

<verification>
1. `len(FEATURE_COLS_V2) == 45` (14 phase3 + 28 phase4 + 3 baselines)
2. `TARGET_COL == "residual"`
3. `"lateness_now" not in FEATURE_COLS_V2`
4. All three _featured_v2.parquet files exist with correct schema
5. `time_to_arrival_seconds` and `baseline_eta` present in all parquets
6. Residual statistics match Phase 7 (mean ~-9s, std ~287s for train)
</verification>

<success_criteria>
- TRAIN-01 satisfied: training pipeline can use residual as target, time_to_arrival_seconds preserved
- TRAIN-02 satisfied: baseline_eta, baseline_s2s, baseline_seg_sum included as features 43-45 (net 45 with lateness_now removed)
- Featured parquets ready for Plan 08-02 (Optuna tuning)
</success_criteria>

<output>
After completion, create `.planning/phases/08-training-adaptation/08-01-SUMMARY.md`
</output>
