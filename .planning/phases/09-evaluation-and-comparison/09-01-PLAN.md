---
phase: 09-evaluation-and-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/evaluate_v1_1.py
  - reports/v1_1_evaluation.html
autonomous: true

must_haves:
  truths:
    - "HTML report exists at reports/v1_1_evaluation.html and opens in a browser"
    - "Console output shows headline MAE comparison: baseline-only (111.1s), v1.0 (123.1s), v1.1 (94.6s)"
    - "Per-route table shows all 23 routes with v1.0 MAE, v1.1 MAE, delta, and % improvement"
    - "Route 27 is flagged with asterisk for low sample count (96 samples)"
    - "SHAP side-by-side bar charts show v1.0 top 15 and v1.1 top 15 features"
    - "Residual histogram overlays predicted vs actual residuals with summary stats"
    - "MAE vs stops_away and MAE vs hour line charts appear in report"
    - "Tail analysis shows % of predictions >5min and >10min off"
  artifacts:
    - path: "scripts/evaluate_v1_1.py"
      provides: "Complete v1.1 evaluation pipeline with HTML report generation"
      min_lines: 300
    - path: "reports/v1_1_evaluation.html"
      provides: "Self-contained HTML evaluation report with embedded charts"
  key_links:
    - from: "scripts/evaluate_v1_1.py"
      to: "models/tuned_metrics.json"
      via: "json.load for v1.0 per-route metrics"
      pattern: "tuned_metrics\\.json"
    - from: "scripts/evaluate_v1_1.py"
      to: "models/v1_1_metrics.json"
      via: "json.load for v1.1 per-route metrics"
      pattern: "v1_1_metrics\\.json"
    - from: "scripts/evaluate_v1_1.py"
      to: "models/tuned_v1.ubj"
      via: "xgb.Booster load for SHAP analysis"
      pattern: "tuned_v1\\.ubj"
    - from: "scripts/evaluate_v1_1.py"
      to: "models/v1_1_residual.ubj"
      via: "xgb.Booster load for SHAP analysis and live predictions"
      pattern: "v1_1_residual\\.ubj"
    - from: "scripts/evaluate_v1_1.py"
      to: "data/processed/test_featured_v2.parquet"
      via: "load_featured_v2('test') for live predictions"
      pattern: "load_featured_v2"
---

<objective>
Build the definitive v1.1 vs v1.0 evaluation report proving v1.1 (94.6s MAE) beats v1.0 (123.1s MAE).

Purpose: This is the final deliverable of the v1.1 milestone -- a self-contained HTML report with side-by-side comparison, per-route breakdown, SHAP feature importance analysis, and residual diagnostics. It proves the residual-target rearchitecture was worth it.

Output: `scripts/evaluate_v1_1.py` (evaluation script) and `reports/v1_1_evaluation.html` (self-contained HTML report with embedded matplotlib charts).
</objective>

<execution_context>
@C:\Users\ryanp\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\ryanp\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-evaluation-and-comparison/09-CONTEXT.md
@.planning/phases/09-evaluation-and-comparison/09-RESEARCH.md

Key source files to reference:
@scripts/evaluate.py (Phase 6 v1.0 evaluation -- patterns to follow, not reuse directly)
@scripts/build_differentiator_features.py (FEATURE_COLS_V2, PHASE3_FEATURE_COLS, PHASE4_FEATURE_COLS, CATEGORICAL_COLS_V2, load_featured_v2, TARGET_COL, KEEP_EXTRA)

Data files:
- models/tuned_metrics.json (v1.0 per-route MAE/RMSE, best_iteration=2158)
- models/v1_1_metrics.json (v1.1 per-route reconstructed_mae/rmse)
- models/tuned_v1.ubj (v1.0 model, 43 features)
- models/v1_1_residual.ubj (v1.1 model, 45 features, 274 rounds)
- data/processed/test_featured_v2.parquet (test data)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the v1.1 evaluation script</name>
  <files>scripts/evaluate_v1_1.py</files>
  <action>
Create `scripts/evaluate_v1_1.py` -- a standalone evaluation script that produces both console output and a self-contained HTML report. The script has NO command-line arguments; it runs everything in one shot.

CRITICAL DATA SYNC ISSUE (from RESEARCH.md): The v1.1 model was trained on progress-decile baselines, but `build_baselines.py` was subsequently rewritten. The featured_v2 parquets now contain baselines that don't match training data. Therefore:
- Use saved metrics from JSON files for headline numbers (94.6s, not live-computed ~103s)
- Use live pred_contribs for SHAP analysis (still valid -- explains model behavior)
- Use live v1.1 predictions for residual diagnostics (shape/distribution analysis)
- Document this discrepancy in the report with a note

STRUCTURE (in order of report sections):

1. **Imports and setup:**
   - `matplotlib.use("Agg")` BEFORE importing pyplot (headless rendering)
   - Import from build_differentiator_features: FEATURE_COLS_V2, PHASE3_FEATURE_COLS, PHASE4_FEATURE_COLS, CATEGORICAL_COLS_V2, TARGET_COL, KEEP_EXTRA, load_featured_v2
   - `sys.path.insert(0, str(Path(__file__).resolve().parent))` for imports
   - Helper: `fig_to_base64(fig, dpi=150)` -- saves fig to BytesIO, base64-encodes, closes fig

2. **Data loading function:**
   - Load test_featured_v2.parquet via `load_featured_v2("test")`
   - Load both metrics JSONs: `models/tuned_metrics.json`, `models/v1_1_metrics.json`
   - Load v1.0 model (tuned_v1.ubj): `bst_v1_0 = xgb.Booster(); bst_v1_0.load_model(...)`
   - Load v1.1 model (v1_1_residual.ubj): same pattern
   - Build v1.0 DMatrix: need 43 features. Construct V1_0_FEATURES = PHASE3_FEATURE_COLS + ["lateness_now"] + PHASE4_FEATURE_COLS (lateness_now was at index 6 in old feature list, but position doesn't matter for DMatrix -- just column presence). Actually: the old PHASE3 had lateness_now; current PHASE3 does not. So: `V1_0_FEATURES = PHASE3_FEATURE_COLS[:6] + ["lateness_now"] + PHASE3_FEATURE_COLS[6:] + list(PHASE4_FEATURE_COLS)`. Wait -- simpler: just hardcode the old 15-element Phase 3 list. Copy from research: the old PHASE3 had lateness_now after is_idle. Current PHASE3_FEATURE_COLS has 14 elements. Insert lateness_now to make 15. Then V1_0_FEATURES = old_phase3 + PHASE4_FEATURE_COLS = 15 + 28 = 43. Add `df["lateness_now"] = 0.0`.
   - Build v1.1 DMatrix: FEATURE_COLS_V2 (45 features), straightforward
   - v1.0 predictions: `bst_v1_0.predict(dtest_v1_0, iteration_range=(0, 2159))`
   - v1.1 predictions: `bst_v1_1.predict(dtest_v1_1)` (274 rounds, no iteration_range needed)
   - v1.1 reconstructed predictions: `baseline_eta + predicted_residual` (baseline_eta from KEEP_EXTRA column in parquet)

3. **Section 1 - Headline Comparison (EVAL-01, EVAL-02):**
   - THREE-WAY comparison as the report headline:
     - Baseline-only: 111.1s MAE (from v1_1_metrics.json comparison or computed as mean(|baseline_eta - time_to_arrival_seconds|))
     - v1.0: 123.1s MAE (from tuned_metrics.json)
     - v1.1: 94.6s MAE (from v1_1_metrics.json)
   - Display as styled metric cards in HTML
   - Console: print all three with improvement percentages
   - Use values FROM JSON files, not live-computed

4. **Section 2 - Per-Route Comparison Table (EVAL-04):**
   - Load per-route metrics from BOTH JSON files
   - Build table with columns: Route, N, v1.0 MAE, v1.1 MAE, v1.0 RMSE, v1.1 RMSE, Delta MAE, % Improvement
   - Sort by % improvement (best first)
   - Flag routes with N < 200 with asterisk (Route 27: 96 samples)
   - Count wins/losses/ties (1% tolerance)
   - Narrative highlights section: biggest winners, biggest losers, Route 27 callout
   - Grouped bar chart: v1.0 vs v1.1 MAE per route (side-by-side bars, sorted by route ID for the chart)
   - Colors: v1.0 = "#5B8DB8" (blue), v1.1 = "#2D6A4F" (green)

5. **Section 3 - SHAP Analysis (EVAL-03):**
   - Subsample 2000 rows (RandomState(42)) from test set
   - v1.0 SHAP: build DMatrix with V1_0_FEATURES, `bst_v1_0.predict(dsample_v1_0, pred_contribs=True, iteration_range=(0, 2159))`. Drop bias column (last). Compute mean |SHAP| per feature. Get top 15.
   - v1.1 SHAP: build DMatrix with FEATURE_COLS_V2, `bst_v1_1.predict(dsample_v1_1, pred_contribs=True)`. Drop bias column. Mean |SHAP|. Top 15.
   - Verify `contribs.shape[1] == len(features) + 1` for both models
   - Side-by-side horizontal bar charts (fig with 2 subplots): v1.0 top 15 left, v1.1 top 15 right
   - Print top 15 for each model to console

6. **Section 4 - Residual Diagnostics (EVAL-05):**
   - Use LIVE v1.1 predictions (not saved metrics) for distribution analysis
   - Predicted residuals = v1.1 model output (raw, before adding baseline_eta)
   - Actual residuals = df["residual"] (the TARGET_COL from parquet)
   - Reconstruction errors = |reconstructed_prediction - time_to_arrival_seconds| for each row
   - Histogram: predicted residuals overlaid with actual residuals, summary stats box (mean, std, skew, kurtosis via scipy.stats)
   - Tail analysis: % of predictions with |error| > 300s (5 min), > 600s (10 min), > 900s (15 min)
   - MAE vs stops_away line chart: group by stops_away, compute MAE per group, filter groups with >= 50 samples, line plot with secondary axis for sample count bars
   - MAE vs hour line chart: extract hour from minutes_since_midnight (// 60), group by hour, compute MAE, filter >= 50 samples, line plot with sample count bars
   - Use live-computed errors for these breakdowns (not JSON)
   - Add a note in the report about the data sync issue: "Note: Live predictions yield ~103s MAE due to baseline data sync. Saved metrics (94.6s) reflect training-time baselines."

7. **HTML Report Generation:**
   - Use f-string template (not jinja2 -- simpler for single file)
   - HTML_TEMPLATE constant with CSS styling (see RESEARCH.md for template)
   - Body sections built as HTML strings, inserted into template
   - All charts embedded as base64 PNGs via fig_to_base64()
   - Tables as HTML tables with proper styling
   - Metric cards for headline numbers
   - Save to `reports/v1_1_evaluation.html`
   - `Path("reports").mkdir(parents=True, exist_ok=True)` before writing

8. **Console output:**
   - Print section headers with separators
   - Headline: "v1.1 Evaluation Results"
   - Three-way MAE comparison
   - Per-route summary (wins/losses)
   - Top 5 features from each model's SHAP
   - Tail analysis percentages
   - Final: "Report saved to reports/v1_1_evaluation.html"

AVOID:
- Do NOT use seaborn (not installed)
- Do NOT import from scripts/evaluate.py (it uses wrong feature list for current data)
- Do NOT present live-computed v1.1 MAE (~103s) as the official number. Use 94.6s from JSON
- Do NOT save separate image files -- everything embedded in HTML
- Do NOT use shap library's plot functions -- use raw matplotlib with pred_contribs arrays
  </action>
  <verify>
Run `python scripts/evaluate_v1_1.py` and confirm:
1. Script completes without errors
2. Console shows three-way MAE comparison
3. `reports/v1_1_evaluation.html` exists and is > 50KB (contains embedded charts)
4. No import errors or missing dependencies
  </verify>
  <done>
Script runs successfully, console prints headline metrics (baseline 111.1s, v1.0 123.1s, v1.1 94.6s), and self-contained HTML report exists at reports/v1_1_evaluation.html with all 5 sections: headline comparison, per-route table with grouped bar chart, SHAP side-by-side analysis, residual diagnostics with histogram and tail analysis, and MAE breakdown charts by stops_away and hour.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete v1.1 vs v1.0 evaluation report with all visualizations and metrics</what-built>
  <how-to-verify>
    1. Open `reports/v1_1_evaluation.html` in a web browser
    2. Verify headline section shows three metric cards: baseline-only ~111s, v1.0 123.1s, v1.1 94.6s
    3. Scroll to per-route table: all 23 routes present, Route 27 flagged, sorted by improvement
    4. Check grouped bar chart: v1.0 blue bars vs v1.1 green bars for each route
    5. Check SHAP section: two side-by-side horizontal bar charts with top 15 features each
    6. Check residual diagnostics: histogram with overlay, summary stats box, tail analysis table
    7. Check MAE vs stops_away line chart: MAE increases with distance, sample count bars visible
    8. Check MAE vs hour line chart: hourly pattern visible
    9. Verify the data sync note is present in the residual diagnostics section
    10. Confirm narrative highlights call out biggest winners, losers, and Route 27
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 9, or describe any issues with the report</resume-signal>
</task>

</tasks>

<verification>
Phase 9 requirements coverage:
- EVAL-01: Reconstructed predictions MAE (94.6s) lower than v1.0 (123.1s) -- shown in headline
- EVAL-02: Side-by-side report with three MAEs (baseline, v1.0, v1.1) -- headline metric cards
- EVAL-03: SHAP top 15 features for both models -- side-by-side horizontal bar charts
- EVAL-04: Per-route comparison table with wins/losses -- full table + narrative highlights
- EVAL-05: Residual distribution analysis -- histogram, tail analysis, MAE breakdowns

Verify: `python scripts/evaluate_v1_1.py` runs clean, `reports/v1_1_evaluation.html` is self-contained and renderable
</verification>

<success_criteria>
1. v1.1 (94.6s) conclusively shown to beat v1.0 (123.1s) -- 23.2% improvement
2. Per-route table identifies majority of routes improved
3. SHAP comparison reveals feature importance shift between v1.0 and v1.1
4. Residual diagnostics document distribution characteristics (skew, kurtosis, tails)
5. Report is self-contained HTML (no external dependencies) viewable in any browser
</success_criteria>

<output>
After completion, create `.planning/phases/09-evaluation-and-comparison/09-01-SUMMARY.md`
</output>
