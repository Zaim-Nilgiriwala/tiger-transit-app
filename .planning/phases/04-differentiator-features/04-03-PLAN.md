---
phase: 04-differentiator-features
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - scripts/train_differentiator.py
  - models/differentiator_v1.ubj
  - models/differentiator_metrics.json
  - models/differentiator_shap.png
autonomous: true

must_haves:
  truths:
    - "XGBoost model retrained with all Phase 4 differentiator features produces a lower test MAE than the Phase 3 baseline (394.7s)"
    - "Improvement over baseline is logged with exact MAE numbers for both models"
    - "SHAP analysis shows which new features contribute most to predictions"
    - "Per-route and per-bucket sliced metrics are computed for the differentiator model"
  artifacts:
    - path: "scripts/train_differentiator.py"
      provides: "Training script for v2 model with differentiator features"
      contains: "load_featured_v2"
    - path: "models/differentiator_v1.ubj"
      provides: "Trained XGBoost model with Phase 4 features"
    - path: "models/differentiator_metrics.json"
      provides: "Full metrics report with baseline comparison"
      contains: "baseline_mae"
  key_links:
    - from: "scripts/train_differentiator.py"
      to: "scripts/build_differentiator_features.py"
      via: "import load_featured_v2, FEATURE_COLS_V2"
      pattern: "from build_differentiator_features import"
    - from: "models/differentiator_metrics.json"
      to: "models/baseline_metrics.json"
      via: "baseline comparison numbers"
      pattern: "baseline_mae.*394"
---

<objective>
Retrain XGBoost with all differentiator features and measure improvement over the Phase 3 baseline.

Purpose: This is the payoff plan -- training with the enriched feature set and proving it improves accuracy. The Phase 4 success criterion requires test MAE lower than 394.7s with exact numbers logged. SHAP analysis reveals which new features drive improvement.

Output: `scripts/train_differentiator.py`, `models/differentiator_v1.ubj`, `models/differentiator_metrics.json`, `models/differentiator_shap.png`
</objective>

<execution_context>
@C:\Users\ryanp\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\ryanp\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-differentiator-features/04-CONTEXT.md
@.planning/phases/04-differentiator-features/04-02-SUMMARY.md
@scripts/train_baseline.py
@scripts/build_differentiator_features.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create differentiator training script and retrain model</name>
  <files>scripts/train_differentiator.py, models/differentiator_v1.ubj, models/differentiator_metrics.json, models/differentiator_shap.png</files>
  <action>
Create `scripts/train_differentiator.py` following the same pattern as `train_baseline.py`:

1. **Import from build_differentiator_features:**
   - `from build_differentiator_features import load_featured_v2, FEATURE_COLS_V2, CATEGORICAL_COLS_V2, TARGET_COL`

2. **Same XGBoost hyperparameters as baseline** (to isolate feature impact):
   - objective: reg:squarederror
   - eval_metric: mae
   - tree_method: hist
   - max_depth: 5, learning_rate: 0.05, min_child_weight: 30
   - subsample: 0.8, colsample_bytree: 0.8
   - reg_alpha: 1.0, reg_lambda: 1.0, gamma: 0.1
   - BUT increase num_boost_round to 3000 (baseline was under-trained at 2000 per decision [03-02])
   - Enable early stopping with patience 100 on validation MAE

3. **Load data and create DMatrix:**
   - Load train/val/test via load_featured_v2()
   - Enable categorical feature support in DMatrix (same as baseline)
   - Verify feature count matches FEATURE_COLS_V2

4. **Train:**
   - Train with early stopping on val MAE
   - Log best iteration number and final train/val MAE

5. **Evaluate on test set:**
   - Compute overall MAE, RMSE, MAPE
   - Compute naive baseline MAE (using scheduled_time_to_target as prediction)
   - Compute sliced metrics (same slicing as train_baseline.py):
     - By route_id
     - By stops_away bucket (1, 2-3, 4-6, 7+)
     - By time-of-day (morning/midday/afternoon/evening using Central Time hour)
     - By distance bucket

6. **SHAP analysis:**
   - Use pred_contribs (same approach as train_baseline.py)
   - Compute mean absolute SHAP values per feature
   - Save bar chart to models/differentiator_shap.png
   - Highlight which NEW Phase 4 features appear in top 10

7. **Baseline comparison:**
   - Load models/baseline_metrics.json to get Phase 3 numbers
   - Print side-by-side comparison:
     - Overall MAE: baseline vs differentiator (absolute and % improvement)
     - Per-route MAE comparison
     - Per-bucket MAE comparison
   - Log all comparison data in differentiator_metrics.json under a "comparison" key

8. **Save artifacts:**
   - models/differentiator_v1.ubj (trained model)
   - models/differentiator_metrics.json (full metrics + comparison)
   - models/differentiator_shap.png (feature importance)

9. **Print clear summary:**
   ```
   === PHASE 4 RESULTS ===
   Baseline MAE:        394.7s
   Differentiator MAE:  XXX.Xs
   Improvement:         XX.X% (XXX.Xs reduction)
   Naive MAE:           708.9s
   vs Naive:            XX.X% improvement

   Top 5 features (SHAP):
   1. [feature] - [value]
   ...

   New Phase 4 features in top 10:
   - [feature] (rank X)
   ...
   ```
  </action>
  <verify>
Run `python scripts/train_differentiator.py`. Verify:
- Training completes with early stopping (should stop before 3000 rounds)
- Test MAE is reported and is LOWER than 394.7s (Phase 3 baseline)
- models/differentiator_v1.ubj exists
- models/differentiator_metrics.json exists with comparison data
- models/differentiator_shap.png exists showing feature importance
- SHAP analysis includes all new Phase 4 features
- Per-route and per-bucket sliced metrics are present in metrics JSON
- Naive baseline MAE matches ~708.9s from Phase 3
  </verify>
  <done>
Differentiator model trained with test MAE lower than baseline 394.7s. Improvement logged with exact numbers. SHAP analysis identifies top contributing Phase 4 features. All artifacts saved. Phase 4 success criteria met.
  </done>
</task>

</tasks>

<verification>
1. differentiator_v1.ubj model file exists
2. differentiator_metrics.json contains overall + sliced metrics + baseline comparison
3. Test MAE < 394.7s (baseline) -- this is the Phase 4 gate
4. SHAP plot saved showing feature importance ranking
5. Comparison table in metrics JSON shows improvement per route and per bucket
</verification>

<success_criteria>
- Test MAE is lower than 394.7s (Phase 3 baseline)
- Exact improvement percentage is logged
- SHAP shows which Phase 4 features contribute
- All metrics artifacts saved for Phase 5/6 consumption
- Progressive improvement chain documented: naive (708.9s) -> baseline (394.7s) -> differentiator (XXX.Xs)
</success_criteria>

<output>
After completion, create `.planning/phases/04-differentiator-features/04-03-SUMMARY.md`
</output>
