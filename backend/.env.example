# Database
DATABASE_URL="postgresql://transit:transit_dev@localhost:5432/tigertransit?schema=public"

# Redis
REDIS_URL="redis://localhost:6379"

# Server
PORT=3000
NODE_ENV=development

# GPS Settings
GPS_ENABLED=false
GPS_PROVIDER=mock

# CORS
CORS_ORIGIN=http://localhost:19006

# API Keys (add when needed)
# GPS_API_KEY=your_gps_api_key_here
